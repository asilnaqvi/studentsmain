<%= simple_form_for @student,html: { multipart: true } do |f| %>
<!%= select_tag(:category_id,options_for_select(@boards),:prompt='LESSER THAN KA SIGN AYEGA YAHAN PE AFTER REMOVING THE COMMENT"Select Category") %>
 <%= f.input :full_name, label: "Full Name",class: "input" %>
  <%= f.input :contact_no, label: "Contact No.",class: "input" %>
  <br>
  <%= f.input :admission_year, label: "Admission Year",class: "input" %>
  <br>
   <%= f.input :qualification, label: "Qualification",class: "input" %>
  <br>
      <%= f.input :about_us, label: "About",class: "input" %>
<br>
  <%= f.association :schools,   as: :check_boxes %>
  <br>
  <label class="password required">Upload an image</label> 
  <%= f.file_field :student_img %>
  <br>
  <br>
  <%= f.button :submit, 'Teach!',class: "button" %>
  <br>
<% end %>